import sysconfig, os, glob; v = sysconfig.get_config_vars(); so_paths = glob.glob(f"{v['LIBDIR']}/*{v['LDLIBRARY']}*"); so_paths.extend(glob.glob(f"{v['LIBDIR']}/*/*{v['LDLIBRARY']}*")); print(list(filter(os.path.exists, so_paths))[0])